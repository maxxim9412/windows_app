# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Платформенные папки (сгенерируются `flutter create .`)
# Если решишь версионировать их — убери из игнора.
# macos/ и windows/ версионируются: без них нечего собирать в CI
# (windows-build.yml), а macos/ несёт ручные правки (Info.plist,
# entitlements, bundle id) — «flutter create» их не восстановит.
/android/
/ios/
/linux/
/web/

# Firebase
.firebase/
